{
  "name": "@om-agent-os/example-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@om-agent-os/core": "workspace:*",
    "@om-agent-os/plugin-sdk": "workspace:*"
  }
}
